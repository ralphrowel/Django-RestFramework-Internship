# docker/docker-compose.yml
services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: django_web
    ports:
      - "8000:8000"
    volumes:
      - ..:/app     # live-reload your local code
    env_file:
      - .env
